#include <stdlib.h>
#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 * @n: the integer to compute absolute value of
 *
 * Return: Returns 0
 */
int _abs(int n)
{
	return (abs(n));
}
